# You can add more variables to this file as needed, but please be sure you add them to the config.env file as well!

package edge;

our $repo = $ENV{EDGE_REPO};
our $host = $ENV{EDGE_HOST};
our $username = $ENV{EDGE_USERNAME};
our $password = $ENV{EDGE_PASSWORD};
our $database = $ENV{EDGE_DB};
our $inst_version = $ENV{EDGE_INST_VERSION};
our $version = $ENV{EDGE_VERSION};
our $spock = $ENV{EDGE_SPOCK};
our $cluster = $ENV{EDGE_CLUSTER};
our $repset = $ENV{EDGE_REPSET};
our $n1 = $ENV{EDGE_N1};
our $n2 = $ENV{EDGE_N2};
our $n3 = $ENV{EDGE_N3};
our $nodes = $ENV{EDGE_NODES};
our $port1 = $ENV{EDGE_PORT1};
our $port2 = $ENV{EDGE_PORT2};
our $homedir1 = $ENV{EDGE_HOMEDIR1};
our $homedir2 = $ENV{EDGE_HOMEDIR2};
our $chdir1 =  $ENV{EDGE_CHDIR1};
our $chdir2 =  $ENV{EDGE_CHDIR2};
our $node1_name = $ENV{EDGE_NODE1_NAME};
our $node2_name = $ENV{EDGE_NODE2_NAME};
our $repset = $ENV{EDGE_REPSET};
our $set_name = $ENV{EDGE_SETNAME};
our $schema = $ENV{EDGE_SCHEMA};
our $table_name = $ENV{EDGE_TABLE};


# Leave the 1 on the end of this file:

1;


